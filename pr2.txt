# Create admin policy for application

resource "aws_iam_policy" "app_admin_policy" {
  name = "app-admin-policy"

  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect   = "Allow"
      Action   = "*"
      Resource = "*"
    }]
  })
}